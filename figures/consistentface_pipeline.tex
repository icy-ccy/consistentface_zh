\begin{tikzpicture}[
  font=\sffamily\scriptsize,
  >=Latex,
  node distance=5mm and 7mm,
  input/.style={draw=blue!55!black, rounded corners=2pt, fill=blue!4,
    minimum width=22mm, minimum height=10mm, align=center, line width=.55pt},
  encoder/.style={draw=gray!70, rounded corners=2pt, fill=gray!7,
    minimum width=24mm, minimum height=10mm, align=center, line width=.5pt},
  evidence/.style={draw=teal!65!black, rounded corners=2pt, fill=teal!5,
    minimum width=40mm, minimum height=17mm, align=center, line width=.55pt},
  router/.style={draw=orange!75!black, rounded corners=2pt, fill=orange!7,
    minimum width=43mm, minimum height=16mm, align=left, line width=.55pt,
    inner xsep=4pt, inner ysep=3pt},
  backbone/.style={draw=violet!65!black, rounded corners=2pt, fill=violet!5,
    minimum width=28mm, minimum height=22mm, align=center, line width=.6pt},
  output/.style={draw=green!55!black, rounded corners=2pt, fill=green!5,
    minimum width=18mm, minimum height=10mm, align=center, line width=.55pt},
  flow/.style={->, line width=.65pt, draw=black!75},
  evidenceflow/.style={->, line width=.55pt, draw=teal!70!black},
  condflow/.style={->, line width=.55pt, draw=orange!80!black}
]

% Input and encoding paths.
\node[input] (obs) {退化观测 $y$\\\footnotesize 目标人脸观测锚点};
\node[input, below=5mm of obs] (refs) {参考集合 $\mathcal{R}$\\\footnotesize 外部身份条件};
\node[input, below=5mm of refs] (text) {文本提示 $T$\\\footnotesize 外部属性条件};

\node[encoder, right=8mm of obs] (control) {ControlNet\\结构条件 $C_y$};
\node[encoder, right=8mm of refs] (renc) {身份/视觉编码\\参考 token $V$};
\node[encoder, right=8mm of text] (tenc) {文本与属性槽编码\\$U,\{t_a\}$};

% Evidence estimator and two-level routing.
\node[evidence, right=9mm of renc] (ev) {\textbf{多源证据估计}\\[1pt]
  $g=[s_{RT},s_{yR},s_{yT},s_{RR},q]$\\
  槽级证据 $g_a$};

\node[router, right=9mm of ev, yshift=8mm] (global) {\textbf{全局条件分配} $\;\alpha,c$\\
  $F_g=c[\alpha V+(1-\alpha)U]+(1-c)B_g$};
\node[router, right=9mm of ev, yshift=-12mm] (slot) {\textbf{属性槽级分配} $\;\alpha_a,c_a$\\
  $F_a=c_a[\alpha_a v_a+(1-\alpha_a)t_a]+(1-c_a)B_a$};

\node[encoder, right=8mm of global, yshift=-10mm, minimum height=20mm] (fused)
  {条件集合\\$F=\{F_g,F_1,\ldots,F_A\}$};
\node[backbone, right=8mm of fused] (denoise) {SDXL 去噪主干\\[2pt]
  $z_\tau$\\$+\;C_y\; +\;F$};
\node[output, right=7mm of denoise] (out) {恢复结果\\$\hat{x}$};

% Main flows.
\draw[flow] (obs) -- (control);
\draw[flow] (refs) -- (renc);
\draw[flow] (text) -- (tenc);
\draw[evidenceflow] (renc) -- (ev);
\draw[evidenceflow] (tenc.east) -- ++(4mm,0) |- (ev.south west);
\draw[evidenceflow] (obs.east) -- ++(3mm,0) |- (ev.north west);

\draw[condflow] (ev.east) -- ++(3mm,0) |- (global.west);
\draw[condflow] (ev.east) -- ++(3mm,0) |- (slot.west);
\draw[condflow] (renc.east) -- ++(5mm,0) |- (global.west);
\draw[condflow] (tenc.east) -- ++(7mm,0) |- (slot.west);
\draw[condflow] (global.east) -- ++(3mm,0) |- (fused.west);
\draw[condflow] (slot.east) -- ++(3mm,0) |- (fused.west);
\draw[flow] (fused) -- (denoise);
\draw[flow] (control.east) -- ++(5mm,0) |- (denoise.north west);
\draw[flow] (denoise) -- (out);

\end{tikzpicture}
